class Solution 
{
    //Function to reverse words in a given string.
    String reverseWords(String S)
    {
        // code here 
        String s="";
        String arr[]=S.split("[.]+");
        for(int i=arr.length-1;i>=0;i--)
        {
            if(i!=0)
                s+=arr[i]+".";
            else 
                s+=arr[i];
        }
        return s;
    }   
}
